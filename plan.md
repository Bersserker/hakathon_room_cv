В VS Code / Cursor:нажми Cmd+Shift+V
Для превью рядом с исходником: Cmd+K, потом V

# Критичные неясности и рабочие допущения

## 1) Несогласованность числа классов и диапазона `Predicted`

**Как проверить / снять риск**
В первый день проверить:

* `train_df.result.nunique()` и список уникальных классов;
* соответствие `result -> label`;
* `sample_submission.csv`;
* локальную проверку диапазона `0..18`.

________________________________________________________________________________________________

## 2) Неясный статус `val_df`

**Проблема**
`val_df` дан как валидация (набор данных для проверки качества), но непонятно: это официальный отдельный набор без пересечений или просто еще одна размеченная часть данных.

**Почему это важно**
От этого зависит, как строить работу:

* либо мы вообще не трогаем `val_df` до конца;
* либо объединяем его с train и сами делим данные на части.

**Рабочее допущение**
Основной вариант такой:

* `train_df` — для обучения и внутреннего деления на части через `StratifiedGroupKFold` по `item_id`;
* `val_df` — отдельная контрольная проверка, которую не используем для подбора настроек до выбора финального кандидата.

**Как проверить / снять риск**
Проверить пересечения по:

* `item_id`;
* `image_id_ext`;
* hash изображения (цифровой отпечаток картинки).

Если пересечения есть, нужно заново делить весь размеченный набор и зафиксировать свой `shadow_v1` (свой контрольный набор).

---

## 3) Leakage через `item_id` и связанные изображения объявления

**Проблема**
У одного объявления может быть несколько изображений. Они могут быть очень похожи. Если часть попадет в обучение, а часть в проверку, локальная метрика будет завышена.

**Почему это важно**
Это самый вероятный способ получить красивую локальную оценку и слабый итоговый результат.

**Рабочее допущение**
`item_id` используем только для:

* деления данных на группы;
* проверки утечек;
* удаления дублей.

`item_id` не используем:

* как признак для модели;
* в постобработке;
* в голосовании по объявлению.

**Как проверить / снять риск**

* использовать `StratifiedGroupKFold` по `item_id`;
* искать дубликаты и почти дубликаты через hash/pHash (способы сравнения изображений);
* сделать отдельный отчет по утечкам.

---

## 4) Семантика `ratio` — после исследования выглядит как confidence/consensus score

`ratio` лучше понимать как **уровень уверенности / согласия разметки**
(то есть насколько люди, которые размечали данные, были согласны между собой).

Как применять `ratio`:

**1. Взвешивание — основной вариант**

`loss = CE(y_pred, label) * ratio`
Это уменьшает влияние шумных примеров.

**2. Фильтрация на старте обучения**

`train = df[df.ratio >= 0.6]`
Сначала учим модель на более “чистых” данных.

**3. Взвешенная валидация**

`f1_weighted = f1(..., sample_weight=ratio)`
Так оценка качества будет честнее.

**4. Опционально — soft labels**
(мягкие метки, когда используется не один класс, а распределение вероятностей)

* агрегировать по `item_id`;
* учить модель на распределении классов.

---

## 5) Неясная доступность дополнительных признаков на всех сплитах

**Проблема**
В описании упомянуты `smart-crop`, `text detector`, `face detector`, `Perform`, но в train/val их структура явно не показана, а в test `Perform` указан отдельно.

**Почему это важно**
Если эти признаки есть только в test или сильно отличаются между train и test, мультимодальный подход (когда модель использует не только картинку, но и доп. данные) может работать неправильно.

**Рабочее допущение**
Используем только те группы признаков, у которых есть:

* одинаковая структура в train/val/test;
* покрытие не ниже примерно 95%;
* стабильное соединение по `image_id_ext`.

Если этого нет, убираем эту ветку и оставляем только работу по изображению.

**Как проверить / снять риск**
Сделать `feature_coverage_report`:

* число строк по каждому набору;
* доля пропусков;
* проверка ключей для соединения;
* совпадает ли схема train-test.

---

## 6) Качество эвристических датасетов для `кабинет`, `детская`, `гардеробная`

**Проблема**
Эти данные почти наверняка шумные и собраны по простым правилам. Там могут быть:

* дубликаты;
* ложные примеры;
* пересечения с основным train.

**Почему это важно**
Они могут помочь редким классам, но так же легко могут испортить общее качество.

**Рабочее допущение**
Эти данные считаем **слабыми метками**
(то есть полезными, но не полностью надежными) только для классов `5`, `6`, `11`:

* не используем их в валидации;
* не смешиваем с основным train без снижения веса;
* используем либо с меньшим весом, либо на отдельном этапе дообучения.

**Как проверить / снять риск**

* удалить дубли по `image_id_ext`, `item_id_ext`, hash;
* вручную посмотреть случайную выборку;
* сделать отдельный отчет, что изменилось по классам `5/6/11` и по общему Macro F1.

---

## 9) Не указан compute budget

**Проблема**
Непонятно, сколько реально доступно GPU и какая есть скорость обучения.

**Почему это важно**
От этого зависит:

* какую модель выбирать;
* какой размер изображения использовать;
* сколько делать фолдов (разбиений);
* можно ли позволить ансамбли (объединение нескольких моделей).

**Рабочее допущение**
Планируем минимум под:

* 1 GPU на 24 ГБ памяти или аналог;
* последовательный запуск фолдов;
* если памяти не хватает — уменьшаем размер изображения и берем более легкую модель, а не ломаем весь пайплайн (процесс обучения и инференса).

**Как проверить / снять риск**
В первый день снять benchmark (замер производительности):

* скорость обработки;
* batch size (сколько изображений за раз);
* время одной эпохи;
* оценку времени на baseline и на вторую модель.

---

# 1. Стратегия проекта

1. **Сначала запускаем не самую сложную модель, а самую надежную базовую модель только по изображению.**
   Основной вариант: `ConvNeXt-Tiny` или `EfficientNetV2-S` с дообучением на `train_df`, разбиением `StratifiedGroupKFold` по `item_id`, базовыми аугментациями (простые изменения картинки для устойчивости), mixed precision (экономия памяти), фиксированным seed (фиксированная случайность).

   Почему: это быстро, стабильно, воспроизводимо и дает честную стартовую точку.
   **Запасной вариант:** `EfficientNet-B3/300` или другая более легкая модель, если ресурсов не хватает.

2. **Параллельно в тот же день считаем дешевый базовый вариант на табличных и служебных признаках.**
   Это не основной финальный путь, а страховка и быстрая проверка. Берем `Perform`, `crop_area`, `is_catalog`, `n_texts`, `person_found`, простые метаданные изображения и обучаем `CatBoost` или multinomial logistic regression (модель для классификации нескольких классов).

   Зачем: быстро понять, сколько сигнала уже есть в метаданных, и раньше поймать ошибки соединения таблиц или утечки, не тратя GPU-время.

3. **После baseline проверяем гипотезы строго в таком порядке.**

   `H1` — учитывать `ratio` и работать с дисбалансом классов.
   `H2` — использовать слабые метки для `кабинет/детская/гардеробная`.
   `H3` — объединить выходы image model с табличными признаками (`Perform`, detector, crop).
   `H4` — вторая независимая image model и/или двойной вход: полное изображение + smart-crop.
   `H5` — только потом ансамбль и TTA (усреднение предсказаний на разных вариантах картинки).

   Это порядок по ожидаемой пользе для Macro F1 относительно затрат времени.

4. **Метрика управляет всем.**
   Оптимизируем Macro F1 на 19 классах. Значит:

   * смотрим F1 по каждому классу на каждом хорошем запуске;
   * редкие классы важнее, чем красивое среднее по частым;
   * класс с `F1=0` — это проблема, а не нормальная ситуация.

5. **Работа с дисбалансом**
   Сначала используем ограниченные веса классов в функции потерь. Если этого мало, добавляем class-aware sampler (выборку с учетом редкости классов). Не включаем сразу все тяжелые приемы вместе: sampler, focal loss, агрессивный oversampling. Это часто ломает стабильность.

   Цель: поднять качество редких классов без ухудшения обычных и без большого расхождения между OOF и shadow holdout.

6. **`ratio` используем как сигнал шума.**
   Практический маршрут:

   * аудит по диапазонам `ratio`;
   * линейное или ограниченное взвешивание примеров;
   * если нужно — двухэтапное обучение: сначала на надежных данных, потом на всем train.

   Это проще и надежнее, чем сразу строить сложное распределение классов.

7. **Эвристические датасеты — это точечный инструмент для редких классов.**
   Их место — отдельная ветка улучшения редких классов: расширение train для `5/6/11` с пониженным весом, строгим удалением дублей и отдельной оценкой.

   Нельзя просто смешивать их с основным train и нельзя использовать их в validation.

8. **Дополнительные признаки подключаем через late fusion, а не через тяжелую общую модель.**
   Основной путь:

   * image model дает logits/probabilities (оценки по классам);
   * табличная ветка дает дополнительные сигналы;
   * сверху ставим stacker (обычно logistic regression или CatBoost), обученный на OOF-предсказаниях без утечки.

   Почему так: это быстрее проверить, проще воспроизвести и меньше шанс сломать код обучения.
   **Запасной вариант:** если покрытие признаков плохое или прирост нестабилен — эту ветку просто убираем.

9. **Smart-crop используем не вместо полной картинки, а как второй взгляд.**
   Для части классов crop может убрать лишний шум, но для других важен весь контекст комнаты. Поэтому:

   * baseline — полное изображение;
   * smart-crop — либо вторая модель, либо отдельный признак в stacker;
   * `is_catalog` — отдельный флаг шума или особого случая.

10. **Ансамбль делаем только если прирост доказан и повторяем.**
    Идем в ансамбль, если:

* есть минимум два реально разных кандидата;
* они ошибаются по-разному;
* прирост по OOF не меньше примерно `0.7 pp` (процентных пункта);
* прирост подтверждается на shadow holdout;
* инференс и упаковка решения остаются управляемыми.

**Не имеет смысла:** усреднять почти одинаковые модели или гнаться за очень маленьким выигрышем ценой усложнения demo, README и защиты.

11. **Воспроизводимость — это отдельная обязательная часть, а не “сделаем потом”.**
    С первого дня фиксируем:

* `split_v1`;
* версию данных и hash;
* обучение и инференс через конфиги;
* MLflow;
* release bundle (финальный пакет артефактов);
* Docker и README.

Любой сильный запуск без commit/config/artifact не считается кандидатом в релиз.

12. **Для защиты нужны не только цифры, но и понятная история.**
    В материалах обязательно должно быть:

* почему baseline именно такой;
* как убрали утечки;
* что реально улучшило результат;
* где модель ошибается;
* почему решение можно повторить локально;
* кто за что отвечал;
* какую пользу дает room-type classification для структуры объявлений, поиска, фильтров и качества каталога.

### Гипотезы с самым высоким ожидаемым влиянием на Macro F1

1. **Расширение weak-label для `кабинет`, `детская`, `гардеробная`.**
   Это прямой способ помочь редким классам, для которых уже есть специальные источники. Ожидается сильный вклад именно в F1 этих классов.

2. **Late fusion: image logits + `Perform`/detector/crop features.**
   Эти признаки могут особенно помочь на спорных классах: интерьер, внешний вид, технические помещения, каталожные фото.

3. **Учет `ratio` / обучение сначала на надежных данных.**
   Если разметка шумная, это часто дает более честный и устойчивый результат, чем еще одна image model.

4. **Аккуратная работа с дисбалансом: сначала веса, потом sampler при необходимости.**
   Для Macro F1 это обязательно: одна-две редкие группы с нулевым F1 портят всю метрику.

5. **Разнообразный ансамбль: лучший image-only + fusion или dual-view.**
   Это не первый шаг, но часто дает последний стабильный прирост, если модели ошибаются по-разному.

---

# 2. План по этапам

Берем горизонт **12 рабочих дней**. Это реалистично для команды из 6 человек, если есть хотя бы 1 GPU 24 ГБ или аналог и уже готов датасет/архив.

| Этап                                           |     Дни | Цель                                   | Что делаем                                                                                                     | Артефакт на выходе                                          | Кто отвечает        | Зависимости                    | Что можно делать параллельно     | Риск                           |
| ---------------------------------------------- | ------: | -------------------------------------- | -------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- | ------------------- | ------------------------------ | -------------------------------- | ------------------------------ |
| 0. Kickoff и технический устав                 |      Д1 | Зафиксировать правила                  | Роли, приоритеты, quality gates, список разрешенных моделей и лицензий, структура репозитория                  | `tech_charter.md`, `model_whitelist.md`, `decision_log.md`  | У1, У5              | Бриф                           | Начать кэшировать данные         | Разрастание scope              |
| 1. Загрузка и аудит данных                     |   Д1–Д2 | Понять реальное состояние данных       | Кэш изображений, manifest, распределение классов, missing/corrupted, статистика по `ratio`                     | `data_manifest.parquet`, `data_integrity.md`, EDA-ноутбук   | У2                  | Доступ к CSV/архиву            | Repo/MLflow, demo skeleton       | Битые изображения              |
| 2. Split и защита от leakage                   |      Д2 | Зафиксировать честную проверку         | Проверка пересечений `train/val`, group split по `item_id`, удаление дублей по hash, policy для shadow holdout | `splits_v1.json`, `leakage_report.md`                       | У2, У1              | Manifest                       | Базовый train loop               | Ложный рост качества           |
| 3. Базовые pipeline                            |   Д2–Д3 | Поднять train/infer end-to-end         | Скрипт image-only, простой baseline на метаданных, validator для submission                                    | `train_image.py`, `infer.py`, `metadata_baseline.md`        | У3, У4, У5          | Split v1                       | README template, figure template | Сломанный train/infer          |
| 4. Baseline quality gate                       |   Д3–Д4 | Получить честную стартовую точку       | 1-fold smoke, потом полный CV, оценка на shadow holdout                                                        | `oof_baseline_v1`, `baseline_metrics_v1.md`                 | У3, У1              | Этап 3                         | Weak-label prep                  | Недооценка compute             |
| 5. Работа с дисбалансом и `ratio`              |   Д4–Д5 | Поднять редкие классы без хаоса        | Weighted loss, sampler ablation, weighting по `ratio`, обучение сначала на надежных данных                     | `ablation_loss_ratio_v1.csv`, `rare_class_board_v1.csv`     | У3, У2              | Baseline OOF                   | Fusion table                     | Перекомпенсация редких классов |
| 6. Weak-label ветка                            |   Д4–Д6 | Усилить `5/6/11`                       | Очистка weak labels, дедуп, класс-специфичное дообучение/смешивание                                            | `weak_labels_v1.parquet`, `weak_label_uplift_v1.md`         | У2, У4              | Аудит данных, split policy     | Baseline / 2-й backbone          | Шум в weak labels              |
| 7. Доп. признаки и fusion                      |   Д5–Д7 | Проверить прирост от мультимодальности | Соединить `Perform`, `crop`, `text`, `face`, собрать fusion-table, обучить stacker на OOF                      | `fusion_table_v1.parquet`, `stacking_eval_v1.md`            | У4                  | OOF logits, покрытие признаков | 2-й backbone, demo UI            | Разная схема train/test        |
| 8. Второй image-кандидат / smart-crop branch   |   Д6–Д8 | Получить независимого кандидата        | Вторая модель другого типа и/или dual-view full+crop                                                           | `oof_model2_v1`, `model2_comparison.md`                     | У3                  | Стабильный baseline            | Fusion, docs                     | Нет реального прироста         |
| 9. Сравнение кандидатов и решение по ансамблю  |   Д8–Д9 | Выбрать основной маршрут               | Сравниваем single model, fusion, weak-label branch, ensemble; смотрим OOF + shadow holdout                     | `rc1_decision.md`, `ensemble_spec.yaml`                     | У1, У3, У4          | Этапы 5–8                      | Подготовка demo                  | Перетюнинг под holdout         |
| 10. Финальный retrain + inference + submission |  Д9–Д10 | Собрать релиз-кандидат                 | Frozen config, retrain на `train+val` если policy разрешает, deterministic infer, генерация CSV и проверка     | `releases/rc1/`, `submission_rc1.csv`                       | У5, У3              | RC decision                    | README, deck                     | Нестабильный infer             |
| 11. Интерпретация и error analysis             |  Д9–Д11 | Подготовить объяснение и материалы     | Confusion matrix, основные ошибки, Grad-CAM, примеры ошибок по классам                                         | `error_taxonomy.md`, figure pack                            | У6, У4              | RC или near-RC                 | Demo, report                     | Слабая интерпретация           |
| 12. Demo и документация                        | Д10–Д11 | Сделать показываемый продукт           | Streamlit/Gradio app, README, runbook, draft отчета                                                            | `demo/app.py`, `README.md`, `final_report.md`               | У6, У5              | Inference pipeline             | Slides                           | Разрастание scope              |
| 13. Подготовка к защите и freeze               | Д11–Д12 | Закрыть сдачу и питч                   | Презентация, PDF/PPTX, speaker notes, Q&A bank, репетиция, backup demo                                         | `deck.pptx`, `deck.pdf`, `qa_bank.md`, финальная submission | У1, У6, вся команда | Все прошлые этапы              | —                                | Нехватка времени               |

### Критический путь проекта

1. Кэш данных и отчет по их целостности.
2. `split_v1` и отчет по утечкам.
3. Рабочий image-only baseline train/infer.
4. Честный OOF и shadow holdout.
5. Один-два реально подтвержденных улучшения (`ratio/imbalance`, weak labels, fusion).
6. Выбор RC и заморозка конфига.
7. Детерминированный инференс и валидная submission.
8. Freeze README/report/deck/demo.

Все остальное можно урезать раньше, чем критический путь.

---

# 3. Распределение команды на 6 разработчиков

| Участник       | Роль                                     | Зона ответственности                                                            | Ключевые результаты                                                         | Основные интерфейсы                                                                       |
| -------------- | ---------------------------------------- | ------------------------------------------------------------------------------- | --------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| **Участник 1** | **ML Team Lead / Principal ML Engineer** | Качество модели, техмаршрут, quality gates, решения по релизу, защита           | `tech_charter`, quality gates, `rc1_decision`, финальная история для защиты | Получает метрики и артефакты от У2–У6; возвращает приоритеты и go/no-go                   |
| Участник 2     | Data & Validation Engineer               | Данные: manifest, split, leakage, dedup, аудит `ratio`, weak-label manifests    | `data_manifest`, `splits_v1`, `leakage_report`, `weak_labels_v1`            | Передает У3/У4/У5 данные и split policy; согласует с У1                                   |
| Участник 3     | CV Training Engineer                     | Обучение image-only, аугментации, модели, OOF, checkpoints                      | baseline OOF, model2 OOF, ablation по loss/sampler, checkpoints             | Получает split/data от У2 и infra от У5; передает logits/weights У1/У4/У5                 |
| Участник 4     | Multimodal / Feature Engineer            | Табличные признаки, fusion/stacking, uplift по редким классам через weak labels | metadata baseline, `fusion_table`, stacking OOF, отчет по редким классам    | Получает feature coverage от У2 и logits от У3; передает спецификацию У1/У5/У6            |
| Участник 5     | Inference & Reproducibility Engineer     | Инференс, воспроизводимость, MLflow/configs, Docker, release bundle, submission | `infer.py`, validator, `releases/rc1`, Docker, runbook                      | Получает checkpoints/spec от У1/У3/У4; передает runnable pipeline У6 и финальный пакет У1 |
| Участник 6     | Demo & Documentation Engineer            | Demo, интерпретация, README/report, презентация и материалы защиты              | demo app, figure pack, final report, deck, Q&A bank                         | Получает pipeline от У5 и выводы/метрики от У1/У3/У4; возвращает финальные материалы      |

**Владение по ключевым зонам:**

* **Качество модели** — Участник 1
* **Данные** — Участник 2
* **Инференс и воспроизводимость** — Участник 5
* **Demo** — Участник 6
* **Документация и защита** — Участник 6, финальное согласование — Участник 1

---

# 4. Подробный backlog по каждому участнику

Приоритеты:

* **P0** — без этого релиз выпускать нельзя;
* **P1** — сильный вклад в качество и устойчивость;
* **P2** — делать только если критический путь не горит.

## Участник 1

### TL-01. Зафиксировать технический устав проекта

* **Что нужно сделать:** оформить единый документ с quality gates, правилами split, правилами сравнения экспериментов, критериями выбора RC, списком разрешенных моделей/весов и владельцами ролей.
* **Артефакт:** `docs/tech_charter.md`, `docs/model_whitelist.md`, `docs/risk_register.md` → используют У2–У6.
* **Зависимости:** репозиторий от У5, разобран бриф.
* **DOR:** роли команды согласованы; repo создан; список входных файлов известен.
* **DOD:** документы закоммичены; есть критерии baseline/RC, список допустимых моделей, правила anti-leakage и владельцы всех P0-зон.
* **Оценка трудозатрат:** 0.75 дн
* **Приоритет:** P0

### TL-02. Утвердить baseline quality gate

* **Что нужно сделать:** определить, по каким артефактам и метрикам baseline считается принятым: OOF, shadow holdout, per-class F1, список нулевых классов, воспроизводимость инференса.
* **Артефакт:** `docs/quality_gate.md`, шаблон `reports/baseline_review_template.md` → используют У3, У4, У5.
* **Зависимости:** `splits_v1.json` от У2, skeleton train/infer от У3/У5.
* **DOR:** split policy зафиксирована; run naming и MLflow tags заведены.
* **DOD:** документ содержит четкие пороги для baseline и RC; все новые эксперименты сравниваются по одному шаблону.
* **Оценка трудозатрат:** 0.5 дн
* **Приоритет:** P0

### TL-03. Провести отбор гипотез после baseline

* **Что нужно сделать:** собрать обзор baseline, сравнить image-only и metadata baseline, выбрать максимум 3 активные ветки улучшений и закрыть низкоприоритетные исследования.
* **Артефакт:** `reports/exp_priority_board_v1.csv`, обновление `docs/decision_log.md` → получают У3, У4, У6.
* **Зависимости:** baseline OOF от У3, metadata baseline от У4, leakage report от У2.
* **DOR:** есть минимум 3 сравнимых запуска на одном split version; baseline train/infer работает end-to-end.
* **DOD:** у каждой активной ветки есть владелец, ожидаемый эффект, срок и критерий остановки; лишние ветки официально закрыты.
* **Оценка трудозатрат:** 0.75 дн
* **Приоритет:** P0

### TL-04. Выбрать release candidate и зафиксировать релизный маршрут

* **Что нужно сделать:** сравнить лучший single model, fusion, weak-label branch и ensemble по OOF, shadow holdout и воспроизводимости; заморозить RC.
* **Артефакт:** `reports/rc_comparison.md`, `configs/release/rc1.yaml` → получают У5 для релизной сборки и У6 для отчета.
* **Зависимости:** кандидаты от У3/У4, inference dry-run от У5.
* **DOR:** есть минимум 2 полноценных кандидата и один воспроизводимый infer pipeline.
* **DOD:** RC выбран; у него есть frozen config, commit SHA, путь к артефактам, решение по `train+val retrain`, и нет открытых P0-рисков.
* **Оценка трудозатрат:** 1 дн
* **Приоритет:** P0

### TL-05. Сформировать защитную историю и распределить ответы

* **Что нужно сделать:** собрать сценарий выступления на 8 минут, распределить спикеров, назначить владельцев ответов и провести репетицию.
* **Артефакт:** `presentation/pitch_script.md`, `docs/qa_bank.md`, `docs/speaker_map.md` → используют все участники.
* **Зависимости:** final metrics/report от У6, RC от У5, ablations от У3/У4.
* **DOR:** финальные метрики и картинки стабильны; draft презентации готов.
* **DOD:** есть 8-минутный сценарий, карта переходов между спикерами, список вероятных вопросов с владельцами и хотя бы одна репетиция по времени.
* **Оценка трудозатрат:** 1 дн
* **Приоритет:** P1

---

## Участник 2

### DATA-01. Собрать локальный кэш данных и manifest

* **Что нужно сделать:** скачать/распаковать изображения, посчитать hash/checksum, размеры, отметить missing/corrupted и сохранить единый manifest.
* **Артефакт:** `data/processed/data_manifest.parquet`, `reports/data_integrity.md` → передает У3 для train, У5 для infer.
* **Зависимости:** пути хранения от У5, доступ к исходным CSV/архиву.
* **DOR:** структура каталогов определена; доступ к входным данным есть.
* **DOD:** manifest содержит `image_id_ext`, локальный путь, hash, width/height, статус файла; доля missing/corrupted зафиксирована в отчете.
* **Оценка трудозатрат:** 1 дн
* **Приоритет:** P0

### DATA-02. Зафиксировать leakage-safe split v1

* **Что нужно сделать:** проверить пересечения `train_df`/`val_df`, сделать group split по `item_id`, убрать дубли по `image_id_ext` и hash, оформить политику shadow holdout.
* **Артефакт:** `data/splits/splits_v1.json`, `reports/leakage_report.md` → передает У3, У4, У5.
* **Зависимости:** manifest из DATA-01.
* **DOR:** все размеченные строки загружены; manifest готов.
* **DOD:** нет пересечений групп между train folds; документирован статус `val_df`; есть таблица распределения классов по fold’ам и список дублей.
* **Оценка трудозатрат:** 1 дн
* **Приоритет:** P0

### DATA-03. Сделать аудит качества разметки и `ratio`

* **Что нужно сделать:** посчитать число примеров по классам, распределение `ratio`, частоту спорных примеров, список изображений с низким согласием и рекомендации по weighting policy.
* **Артефакт:** `reports/label_quality_ratio.md`, `tables/ratio_bins.csv`, `tables/low_consensus_samples.csv` → используют У1, У3, У4.
* **Зависимости:** labeled data, `splits_v1`.
* **DOR:** `result` и `ratio` распарсены; безопасный split зафиксирован.
* **DOD:** отчет содержит по классам размер, медианный `ratio`, примеры спорных изображений и рекомендацию: не взвешивать / линейно / ограниченно.
* **Оценка трудозатрат:** 0.75 дн
* **Приоритет:** P0

### DATA-04. Подготовить weak-label manifest для `кабинет/детская/гардеробная`

* **Что нужно сделать:** собрать эвристические источники, сопоставить их с классами `5/6/11`, удалить дубли с основным train, добавить source flag и weak weight.
* **Артефакт:** `data/processed/weak_labels_v1.parquet`, `reports/weak_labels_audit.md` → передает У4 и У3.
* **Зависимости:** heuristic CSV, hash-функции из DATA-01, policy от У1.
* **DOR:** файлы heuristics доступны; mapping `dataset -> class_id` утвержден.
* **DOD:** manifest содержит ключ, класс, вес, источник, признак пересечения с основным train; дубли по `image_id_ext`/hash` удалены.
* **Оценка трудозатрат:** 1 дн
* **Приоритет:** P1

### DATA-05. Подготовить отчет по покрытию дополнительных признаков

* **Что нужно сделать:** собрать coverage/missingness по `Perform`, `smart-crop`, `text detector`, `face detector`, проверить совпадение train/val/test.
* **Артефакт:** `reports/feature_coverage_v1.md`, `data/processed/features_index_v1.parquet` → передает У4 и У5.
* **Зависимости:** доступ к дополнительным таблицам.
* **DOR:** список файлов/таблиц с доп. признаками известен; ключ соединения определен.
* **DOD:** по каждой группе признаков есть число строк, покрытие, качество join, решение `green / yellow / red` для использования.
* **Оценка трудозатрат:** 0.75 дн
* **Приоритет:** P1

---

## Участник 3

### CV-01. Реализовать config-driven image-only train loop

* **Что нужно сделать:** написать PyTorch pipeline: dataset, aug, loader, train/eval loop, fold training, checkpointing, MLflow logging, AMP.
* **Артефакт:** `src/train_image.py`, `configs/model/image_baseline_v1.yaml` → используют У1, У5.
* **Зависимости:** `splits_v1` от У2, repo/MLflow skeleton от У5.
* **DOR:** путь к кэшу изображений известен; split policy утверждена; backbone выбран из whitelist.
* **DOD:** `make smoke-train` проходит на debug fold; в MLflow есть loss/metrics; checkpoint сохраняется и читается инференсом.
* **Оценка трудозатрат:** 1.5 дн
* **Приоритет:** P0

### CV-02. Снять OOF для основного baseline

* **Что нужно сделать:** прогнать baseline сначала на 1 fold для smoke, затем на полном CV; сохранить OOF probabilities/logits и отчет с метриками по классам.
* **Артефакт:** `artifacts/oof/baseline_v1/`, `reports/baseline_metrics_v1.md` → передает У1 для quality gate, У4 для stacking, У5 для infer contract.
* **Зависимости:** CV-01, quality gate от У1.
* **DOR:** train loop стабилен; MLflow tags и naming conventions зафиксированы.
* **DOD:** OOF покрывает весь train development pool; есть Macro F1, per-class F1, confusion matrix и shadow holdout metric.
* **Оценка трудозатрат:** 1.5 дн
* **Приоритет:** P0

### CV-03. Проверить гипотезы по дисбалансу и `ratio`-aware training

* **Что нужно сделать:** сравнить `weighted CE`, `balanced sampler`, `ratio weighting`, а при необходимости — обучение сначала на надежных данных, потом дообучение на всем наборе.
* **Артефакт:** `reports/ablation_loss_sampling_ratio_v1.csv` → передает У1 для выбора рабочего рецепта.
* **Зависимости:** baseline OOF от CV-02, ratio audit от У2.
* **DOR:** baseline метрики зафиксированы; есть рекомендация по `ratio`.
* **DOD:** минимум 4 сравнимых запуска на одном split version; в отчете есть разница с baseline и решение “берем / не берем”.
* **Оценка трудозатрат:** 1.5 дн
* **Приоритет:** P1

### CV-04. Обучить второй независимый image-кандидат

* **Что нужно сделать:** поднять вторую модель другой семьи и/или dual-view full image + smart-crop branch, чтобы ошибки отличались от первой модели.
* **Артефакт:** `artifacts/oof/model2_v1/`, `reports/model2_eval_v1.md` → передает У1, У4, У5.
* **Зависимости:** stable train loop, smart-crop policy от У4/У2, benchmark по compute.
* **DOR:** baseline recipe заморожен; есть свободное compute window.
* **DOD:** второй кандидат полностью обучен на development split, имеет OOF/shadow метрики и стандартный export logits.
* **Оценка трудозатрат:** 1.5 дн
* **Приоритет:** P1

### CV-05. Подготовить export logits/probabilities для stacker и ensemble

* **Что нужно сделать:** сделать единый формат OOF/test exports для image-моделей, чтобы У4 и У5 не писали ручной glue-code (склейку).
* **Артефакт:** `src/export_logits.py`, `artifacts/logits/<run_name>/*.parquet` → передает У4 и У5.
* **Зависимости:** CV-02 и CV-04.
* **DOR:** у shortlisted моделей есть checkpoints и OOF.
* **DOD:** для каждого кандидата экспортированы logits/probabilities с единым schema по `image_id_ext`; У4 читает их без ручных исправлений.
* **Оценка трудозатрат:** 0.5 дн
* **Приоритет:** P1

---

## Участник 4

### MM-01. Сделать metadata-only sanity baseline

* **Что нужно сделать:** обучить простой baseline на `Perform` и дополнительных признаках, чтобы измерить силу метаданных и проверить правильность join.
* **Артефакт:** `reports/metadata_baseline_v1.md`, `artifacts/oof/metadata_baseline_v1/` → передает У1.
* **Зависимости:** feature coverage от У2.
* **DOR:** train-side дополнительные признаки доступны с приемлемым покрытием; join key проверен.
* **DOD:** есть OOF Macro F1, per-class F1, важность признаков или коэффициенты и вывод, стоит ли тратить время на fusion.
* **Оценка трудозатрат:** 1 дн
* **Приоритет:** P0

### MM-02. Собрать unified fusion table

* **Что нужно сделать:** объединить image logits, `Perform`, `crop_area`, `is_catalog`, `n_texts`, `person_found`, простые метаданные картинки и подготовить train/test таблицы для stacker.
* **Артефакт:** `data/processed/fusion_table_v1.parquet`, `docs/fusion_schema.md` → передает У5 для финального infer, У6 для документации.
* **Зависимости:** logits export от У3, feature index от У2.
* **DOR:** есть стабильные OOF logits хотя бы одной image model; дополнительные признаки имеют статус green/yellow.
* **DOD:** таблица выровнена по `image_id_ext`, число строк совпадает с OOF/test, policy по пропускам зафиксирована, совпадение схемы подтверждено.
* **Оценка трудозатрат:** 0.75 дн
* **Приоритет:** P0

### MM-03. Обучить late-fusion / stacking модель

* **Что нужно сделать:** обучить stacker на OOF-предсказаниях image model + tabular features без leakage; сравнить logistic regression и CatBoost.
* **Артефакт:** `artifacts/oof/stacking_v1/`, `reports/stacking_eval_v1.md`, `configs/model/stacking_v1.yaml` → передает У1 и У5.
* **Зависимости:** MM-02, logits от У3, split policy от У2.
* **DOR:** OOF доступны; fusion table готова; дисциплина по fold понятна.
* **DOD:** есть прирост или ухудшение относительно лучшего image-only кандидата; отсутствие leakage доказано.
* **Оценка трудозатрат:** 1 дн
* **Приоритет:** P1

### MM-04. Провести ветку rare-class uplift через weak labels

* **Что нужно сделать:** подключить `weak_labels_v1` в класс-специфичное обучение/дообучение, замерить эффект по `5/6/11` и по общему Macro F1.
* **Артефакт:** `reports/weak_label_uplift_v1.md`, при успехе — `configs/model/rareclass_branch_v1.yaml` → передает У1 и У3.
* **Зависимости:** weak labels от У2, базовый trainer от У3.
* **DOR:** weak-label manifest готов; baseline recipe стабилен.
* **DOD:** отчет показывает изменение F1 по `5/6/11`, общее изменение Macro F1 и решение “включаем / отбрасываем”.
* **Оценка трудозатрат:** 1 дн
* **Приоритет:** P1

### MM-05. Подготовить class-wise error taxonomy

* **Что нужно сделать:** разобрать основные пары путаемых классов и привязать ошибки к типам причин: шум разметки, нехватка контекста, визуальная похожесть, каталожные фото, путаница интерьер/экстерьер.
* **Артефакт:** `reports/error_taxonomy_v1.md`, sample grids → передает У6 для отчета и слайдов.
* **Зависимости:** OOF от У3 и MM-03.
* **DOR:** есть финальный shortlist моделей и merged prediction table.
* **DOD:** минимум 10 ключевых пар ошибок описаны с реальными примерами и рекомендацией “лечим моделью / лечим данными / не лечим в этот срок”.
* **Оценка трудозатрат:** 0.75 дн
* **Приоритет:** P2

---

## Участник 5

### PLAT-01. Поднять reproducible repo skeleton

* **Что нужно сделать:** настроить структуру проекта, `Makefile`, `pyproject`, pre-commit, тесты, Hydra/OmegaConf, MLflow backend, общие пути к артефактам.
* **Артефакт:** `Makefile`, `pyproject.toml`, `configs/`, `docs/mlflow_setup.md` → используют все.
* **Зависимости:** нет.
* **DOR:** создан git-репозиторий и согласована структура папок.
* **DOD:** `make setup`, `make lint`, `make test`, `make smoke-train` выполняются на чистой машине.
* **Оценка трудозатрат:** 1 дн
* **Приоритет:** P0

### PLAT-02. Реализовать inference pipeline и submission validator

* **Что нужно сделать:** сделать единый batched infer, флаг TTA, загрузку fusion-features при необходимости, запись CSV и строгую проверку формата.
* **Артефакт:** `src/infer.py`, `src/validate_submission.py`, `submissions/example_valid.csv` → используют У1, У6.
* **Зависимости:** export logits/weights от У3, fusion schema от У4, policy диапазона классов от У1.
* **DOR:** есть хотя бы один обученный checkpoint и стабильный input contract.
* **DOD:** команда вида `python -m src.infer --config configs/release/rc1.yaml` создает CSV; validator проверяет header, `image_id_ext`, уникальность id, перевод в integer и диапазон `0..18`.
* **Оценка трудозатрат:** 1 дн
* **Приоритет:** P0

### PLAT-03. Собрать Docker и runbook

* **Что нужно сделать:** упаковать train/infer/demo в Docker, зафиксировать зависимости, описать запуск локально и в контейнере.
* **Артефакт:** `Dockerfile`, `requirements-lock.txt`, `RUNBOOK.md` → используют У6 и У1.
* **Зависимости:** PLAT-01, рабочие команды train/infer/demo.
* **DOR:** core scripts запускаются локально.
* **DOD:** `docker build` успешен; `docker run ... make infer` дает тот же результат на фиксированном sample, что и локальный запуск.
* **Оценка трудозатрат:** 1 дн
* **Приоритет:** P1

### PLAT-04. Организовать artifact registry и release bundle

* **Что нужно сделать:** собрать единый пакет релиза: checkpoints, config, commit SHA, checksum, лицензии, команды запуска, summary по метрикам.
* **Артефакт:** `artifacts/registry.csv`, `releases/rc1/`, `releases/rc1/SHA256SUMS`, `releases/rc1/LICENSES.md` → передает У1 и У6.
* **Зависимости:** RC решение от У1, checkpoints от У3/У4.
* **DOR:** выбран shortlist моделей; есть финальные веса и config.
* **DOD:** каждый релизный артефакт имеет hash, commit SHA, metric, license note; bundle воспроизводимо поднимается по `RUNBOOK.md`.
* **Оценка трудозатрат:** 0.75 дн
* **Приоритет:** P1

### PLAT-05. Проверить стабильность инференса и time budget

* **Что нужно сделать:** сравнить повторные прогоны, чувствительность к batch size, latency CPU/GPU и устойчивость к отсутствию доп. признаков.
* **Артефакт:** `reports/inference_stability_v1.md` → передает У1 для финального go/no-go.
* **Зависимости:** PLAT-02, RC bundle.
* **DOR:** есть release candidate и финальный infer script.
* **DOD:** повторный infer на одном и том же input дает тот же output; latency и memory budget задокументированы; fallback batch size зафиксирован.
* **Оценка трудозатрат:** 0.5 дн
* **Приоритет:** P2

---

## Участник 6

### DEMO-01. Собрать demo-приложение

* **Что нужно сделать:** поднять Streamlit/Gradio app с загрузкой изображения, top-3 prediction, confidence, Grad-CAM и показом доступных дополнительных сигналов.
* **Артефакт:** `demo/app.py`, `demo/README.md`, screenshots/GIF → передает У1 для защиты.
* **Зависимости:** infer contract от У5, финальный или почти финальный checkpoint от У3/У5.
* **DOR:** есть функция `predict_one()` или аналог; модель упакована в воспроизводимое окружение.
* **DOD:** приложение запускается одной командой; обрабатывает минимум 5 заранее подготовленных примеров и один пользовательский upload; показывает version модели и нормально обрабатывает ошибки.
* **Оценка трудозатрат:** 1.5 дн
* **Приоритет:** P1

### DEMO-02. Подготовить interpretability и visual pack

* **Что нужно сделать:** собрать confusion heatmap, bar chart по per-class F1, примеры Grad-CAM и графики “baseline → final”.
* **Артефакт:** `docs/figures/*`, `notebooks/visuals_final.ipynb` → используют У1 и вся команда в отчете/слайдах.
* **Зависимости:** metrics и taxonomy от У3/У4.
* **DOR:** shortlist моделей известен; есть финальные таблицы метрик.
* **DOD:** figure pack экспортирован в PNG/SVG; у каждой картинки есть подпись и ссылка на run/version.
* **Оценка трудозатрат:** 0.75 дн
* **Приоритет:** P1

### DEMO-03. Собрать финальный README и текстовый отчет

* **Что нужно сделать:** описать архитектуру, данные, защиту от leakage, эксперименты, итоговые метрики, запуск train/infer/demo, вклад участников и ограничения решения.
* **Артефакт:** `README.md`, `docs/final_report.md` или структура Google Doc → передает У1 для финальной загрузки.
* **Зависимости:** RUNBOOK от У5, ablations от У1/У3/У4, финальные figures.
* **DOR:** RC freeze произошел; команды запуска больше не меняются.
* **DOD:** новый разработчик может по README повторить infer без устных пояснений; отчет содержит ссылки на ключевые артефакты и вклад команды.
* **Оценка трудозатрат:** 1 дн
* **Приоритет:** P0

### DEMO-04. Собрать презентацию и speaker notes

* **Что нужно сделать:** подготовить 10–15 слайдов: бизнес-логика, pipeline, данные, anti-leakage, эксперименты, результат, demo, воспроизводимость, вклад участников.
* **Артефакт:** `presentation/final_deck.pptx`, `presentation/final_deck.pdf`, `presentation/speaker_notes.md` → используют все.
* **Зависимости:** final report, figures, pitch map от У1.
* **DOR:** финальные цифры и картинки утверждены.
* **DOD:** deck укладывается в 8 минут, экспортирован в PPTX и PDF, содержит ссылки на текстовый документ и дополнительные материалы.
* **Оценка трудозатрат:** 1 дн
* **Приоритет:** P0

### DEMO-05. Провести репетицию и собрать Q&A банк

* **Что нужно сделать:** провести полную репетицию, собрать вероятные вопросы заказчика, распределить отвечающих, проверить технику и backup-план.
* **Артефакт:** `docs/qa_bank.md`, `docs/rehearsal_checklist.md` → используют все.
* **Зависимости:** draft deck, pitch script от У1.
* **DOR:** deck версии 0.9 готов; спикеры назначены.
* **DOD:** хотя бы одна репетиция проведена по времени; есть владелец ответа на каждый типовой вопрос; назначен backup presenter/demo operator.
* **Оценка трудозатрат:** 0.5 дн
* **Приоритет:** P1

---

# 5. Контрольные точки управления

## Checkpoint’ы и какие артефакты проверяем

| Checkpoint | Когда     | Что проверяем                                                                            | Что решаем                                                  |
| ---------- | --------- | ---------------------------------------------------------------------------------------- | ----------------------------------------------------------- |
| CP0        | Конец Д1  | `tech_charter`, draft `data_manifest`, repo skeleton, список разрешенных моделей         | Фиксируем split policy, naming, приоритеты и compute budget |
| CP1        | Конец Д3  | smoke-train, `infer.py`, `metadata_baseline`, первые baseline metrics                    | Baseline принят или ищем баг в pipeline                     |
| CP2        | Конец Д5  | `baseline_metrics_v1`, `label_quality_ratio`, `ablation_loss_ratio_v1`, `weak_labels_v1` | Оставляем 2–3 реальные ветки улучшения                      |
| CP3        | Конец Д8  | `model2_eval_v1`, `stacking_eval_v1`, `weak_label_uplift_v1`, error taxonomy             | Идем в ансамбль или freeze single model                     |
| CP4        | Конец Д10 | `releases/rc1`, deterministic infer, README draft, demo alpha                            | Фиксируем RC, режем scope если нужно                        |
| CP5        | Конец Д12 | финальная submission, report, deck, PDF, Q&A bank, rehearsal checklist                   | Готовность к сдаче и защите                                 |

## Метрики и таблицы, обязательные для решений

Для каждого серьезного кандидата нужен минимум:

1. **Macro F1 OOF** на фиксированном split version.
2. **Macro F1 на shadow holdout**.
3. **Per-class F1 / precision / recall / support**.
4. **Список классов с `F1=0` или `recall=0`**.
5. **Top confusion pairs**.
6. **Разница между OOF и shadow holdout**.
7. **Таблица по диапазонам `ratio`**.
8. **Отчет по покрытию доп. признаков**.
9. **Время инференса и устойчивость результатов**.
10. **Сравнение с baseline в одном формате таблицы**.

Отдельно для веток по редким классам:

* F1 по классам `5`, `6`, `11`;
* разница с baseline по этим классам;
* цена этого улучшения для общего Macro F1.

## Когда команда принимает ключевые решения

**`оставляем baseline`**

* pipeline train/infer стабилен;
* image-only baseline честно посчитан;
* нет признаков leakage;
* baseline лучше metadata floor или хотя бы объяснимо не хуже на shadow holdout;
* на нем можно надежно строить ablation.

**`идем в ансамбли`**

* есть минимум 2 кандидата с разными ошибками;
* uplift на OOF не меньше примерно `0.7 pp`;
* uplift не исчезает на shadow holdout;
* inference/release bundle остаются управляемыми.

**`режем scope`**

* если ветка дает меньше `0.5 pp` после 1 рабочего дня и 3–4 корректных запусков;
* если покрытие признаков сомнительное;
* если demo/README/RC отстают;
* если лучший run нельзя воспроизвести по config/artifacts.

**`фиксируем релиз-кандидат`**

* выбранный config заморожен;
* deterministic infer проходит повторно без расхождений;
* validator пропускает submission;
* bundle содержит config, weights, hashes, license notes, commands;
* отчет и demo уже опираются на неизменяемый output.

---

# 6. Риски и план снижения

| Риск | Вероятность | Влияние | Как заметим | Как снижаем | Владелец |
|---|---|---|---|---|---|
| Слабое качество разметки | Высокая | Высокое | Низкий `ratio`, нестабильные ошибки, визуально спорные примеры | Проверяем `ratio`, уменьшаем вес шумных примеров, сначала обучаем на более надежных данных, вручную смотрим спорные примеры без изменения исходных меток | У2, У3 |
| Дисбаланс классов | Высокая | Высокое | Есть классы с малым числом примеров и `F1 = 0` | Используем ограниченные веса классов, при необходимости sampler (способ чаще показывать редкие классы), отдельно следим за редкими классами, пробуем weak labels для `5/6/11` | У3 |
| Leakage через `item_id` и дубликаты | Высокая | Критическое | Подозрительно высокий локальный результат, пересечения между train и validation, одинаковые или почти одинаковые изображения | Делаем group split по `item_id`, ищем и удаляем дубли, не используем `item_id` как признак | У2 |
| Переобучение на train/CV | Средняя | Высокое | Слишком большой разрыв между train и OOF, нестабильный результат при разных seed | Используем умеренные аугментации, early stopping (раннюю остановку), честный OOF, проверяем результат на shadow holdout | У3 |
| Плохая обобщающая способность | Средняя | Высокое | Разница между OOF и shadow holdout больше примерно чем `2 pp`, качество по классам сильно меняется | Держим отдельный shadow holdout, осторожно выбираем модель, не используем слишком хрупкие эвристики и лишнюю подгонку | У1 |
| Нестабильность инференса | Средняя | Высокое | Один и тот же запуск дает разные результаты, результат зависит от batch size, ломается при пропусках в признаках | Делаем deterministic infer (повторяемый инференс), фиксируем зависимости, используем validator, держим запасной image-only вариант | У5 |
| Нехватка времени на demo, документацию и защиту | Высокая | Высокое | К Д10 нет черновика README, demo alpha или skeleton deck | Замораживаем scope на Д8–Д10, назначаем отдельного ответственного за demo и документацию, каждый день проверяем не-ML артефакты | У1, У6 |
| Конфликт между качеством модели и воспроизводимостью | Средняя | Критическое | Лучший run существует только в notebook, без config или без весов | Не допускаем в RC сильные запуски без артефактов, финальный release bundle обязателен | У1, У5 |
| Битые или недоступные изображения | Средняя | Среднее | Ошибки загрузки, много missing files, ошибки на инференсе | Делаем локальный кэш, собираем manifest, добавляем fallback handling, делаем отчет по corrupted files | У2 |
| Сомнительное покрытие дополнительных признаков | Средняя | Среднее | Покрытие меньше `95%`, схема train и test не совпадает | Для каждой группы признаков принимаем решение `green/yellow/red`, late fusion оставляем как необязательную ветку | У2, У4 |
| Лицензия pretrained weights не подтверждена | Низкая | Высокое | Нет явного источника или лицензии в registry | Используем whitelist моделей, добавляем `LICENSES.md` в release bundle, запрещаем неподтвержденные веса | У1, У5 |
---

# 7. Что должно быть готово к финальной сдаче

Чеклист ниже закрывает набор артефактов, нужных для финальной сдачи и защиты: код, запуск, веса, submission, текстовый документ, demo, презентация и вклад команды.

* **Код**
  Репозиторий со структурой `src/`, `configs/`, `demo/`, `docs/`, `scripts/`, `tests/`; без критической логики, спрятанной только в ноутбуках.

* **Конфиги**
  Зафиксированные `configs/model/*.yaml`, `configs/data/*.yaml`, `configs/release/rc1.yaml`, `split_v1`.

* **Чекпоинты / веса**
  Финальные веса в `releases/rc1/weights/`, checksum, commit SHA, краткая карточка модели, notes по лицензиям.

* **Inference pipeline**
  Рабочий `src/infer.py`, `src/validate_submission.py`, команда запуска из README, deterministic output на фиксированном sample.

* **Submission**
  Финальный CSV с колонками `image_id_ext,Predicted`, проверенный validator’ом, диапазон классов `0..18`, checksum и дата сборки.

* **README**
  Что это, как повторить train/infer/demo, какие зависимости, где лежат веса, как собрать Docker, ограничения решения.

* **Текстовый отчет / документ**
  Финальный `final_report.md` или Google Doc со ссылками на:

  * данные и split policy;
  * описание моделей;
  * таблицу экспериментов;
  * итоговые метрики;
  * разбор ошибок;
  * demo;
  * роли участников.

* **Demo**
  Streamlit/Gradio app, который запускается локально. Плюс backup: GIF/скринкаст на случай технических проблем.

* **Презентация**
  `final_deck.pptx` и `final_deck.pdf`, 10–15 слайдов, без перегруза текстом, с бизнес-рамкой и ссылкой на текстовый документ.

* **Распределение ролей участников**
  Отдельная таблица в отчете и отдельный слайд в презентации: кто за что отвечал и какой артефакт закрыл.

* **Список вероятных вопросов заказчика и кто на них отвечает**
  `docs/qa_bank.md` с владельцами ответов и короткими тезисами.

* **Artifact registry**
  `artifacts/registry.csv` и `releases/rc1/` с полным следом воспроизводимости.

* **LICENSES / model whitelist**
  Отдельный список используемых моделей и их лицензий, чтобы не было вопросов по ограничениям.

### Вероятные вопросы заказчика и кто на них отвечает

| Вероятный вопрос                                                                |
| ------------------------------------------------------------------------------- |
| Как вы исключили leakage через `item_id` и похожие изображения?                 |
| Почему выбрали именно этот backbone, а не более крупный ViT?                    |
| Что вы сделали с дисбалансом классов и почему это важно для Macro F1?           |
| Как использовали `ratio` и почему не строили full soft labels?                  |
| Что реально дали дополнительные признаки `Perform` / `smart-crop` / detector’ы? |
| Почему weak labels использовали только для `кабинет`, `детская`, `гардеробная`? |
| Насколько решение воспроизводимо и как его запустить локально?                  |
| Что показываете в demo и насколько это устойчиво?                               |
| Где модель все еще ошибается и почему?                                          |
| Какой вклад каждого участника и кто чем владеет?                                |
| Что бы вы делали следующим этапом, если бы была еще одна неделя?                |
| Насколько решение practically useful для Авито?                                 |